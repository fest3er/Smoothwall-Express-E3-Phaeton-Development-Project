#! /bin/bash

  if [ "$1" == "" ]; then
    CMD="/tools/bin/bash -l +h"
  else
    CMD="$*"
  fi

  newroot=`bash ./distrib_root`
  newpath="$PATH:/tools/bin:/build"
  if [ `id -u` -eq 0 ]; then BCMD="bash"; else BCMD="sudo su"; fi
  ${BCMD} -c "ROOT=$newroot; PATH=$newpath; ${CMD}"
