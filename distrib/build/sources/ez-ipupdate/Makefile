include ../Makefile.conf

PACKAGE = ez-ipupdate
VERSION = 3.0.11b8

BASE_URL = http://gentoo.blueyonder.co.uk/distfiles/

INSTALL = yes

install: compile
	@install -D $(COMPILE_DIR)/ez-ipupdate $(PKG_ROOT)/$(PKG_DIR)/bin/ez-ipupdate
	
include ../Makefile.rules
