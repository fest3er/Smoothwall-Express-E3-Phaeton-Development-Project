LIBS = -ldl /usr/lib/libsmoothd.so

MODFLAGS = $(CFLAGS) -fPIC -Wall -shared -Wl,-soname,$@.so -o $@.so $@.cpp $(LIBS)

all: $(PLUGINS)

$(PLUGINS):
	$(CXX) $(MODFLAGS)

clean:
	@echo -n "mods/snmp "
	@rm -f *.o *.so
