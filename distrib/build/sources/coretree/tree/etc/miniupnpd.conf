# WAN network interface 
# the ext_ifname = RED interface
#ext_ifname=eth3 

# enable NAT-PMP support (default is no) 
enable_natpmp=yes 

# there can be multiple listening ips for receiving SSDP traffic. 
# the 1st IP is also used for UPnP Soap traffic.
# the listening_ip is usually the GREEN interface for UPnP clients on GREEN 
#listening_ip=192.168.0.1 
port=5555 

# bitrates reported by daemon in bits per second 
bitrate_up=1000000
bitrate_down=10000000


# default presentation url is http address on port 80 
#presentation_url= 

# report system uptime instead of daemon uptime 
system_uptime=yes 

# notify interval in seconds default is 30 seconds. 
notify_interval=30 

# log packets in pf 
#packet_log=no 

# uuid : generated by the install a new one can be created with 
# uuidgen 
#uuid=

# UPnP permission rules 
# (allow|deny) (external port range) ip/mask (internal port range) 
# A port range is <min port>-<max port> or <port> if there is only 
# one port in the range. 
# ip/mask format must be nn.nn.nn.nn/nn 
allow 1024-65535 0.0.0.0/0 1024-65535
deny 0-65535 0.0.0.0/0 0-65535

